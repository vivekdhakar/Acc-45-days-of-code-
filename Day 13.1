#include <iostream>
using namespace std;

int main() {
	int t,x,n,y;
	cin>>t;
	while(t--)
	{
       cin>>n>>x;
       y=n-x;
       if(x>=y)
       {
           cout<<n-x<<endl;
       }
       else if(y>=x)
       {
           cout<<n-y<<endl;
       }
	}
	return 0;
}
